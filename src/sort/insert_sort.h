#pragma once
// Copyright 2020 yht
// insert_soft
#include "../../inc/type.h"

namespace algor_yht {
namespace sort {
    template<typename T>
    T* insert_sort(const T * input) {
    }
    template<typename T, const typename type::uint_t  N>
    T* insert_sort(const T input[N]) {
    }
}  // sort
}  // glgor_yht
